apiVersion: apps/v1
kind: Deployment
metadata:
  name: candle-server
  namespace: alpha-bot
  labels:
    app: candle-server
spec:
  replicas: 20
  selector:
    matchLabels:
      app: candle-server
  template:
    metadata:
      labels:
        app: candle-server
    spec:
      containers:
      - name: candle-server
        image: europe-west4-docker.pkg.dev/nlc-bot-36685/alpha-bot/candle-server
        imagePullPolicy: Always
        volumeMounts:
          - name: google-cloud-auth
            mountPath: /run/secrets/google-cloud-auth
            readOnly: true
        resources:
          requests:
            memory: "70Mi"
            cpu: "13m"
        env:
          - name: PRODUCTION_MODE
            value: "1"
          - name: NODE_ENV
            value: "production"
          - name: IEXC_KEY
            valueFrom:
              secretKeyRef:
                name: alpha-credentials
                key: IEXC_KEY
        ports:
          - containerPort: 6900
      volumes:
        - name: google-cloud-auth
          secret:
            secretName: google-cloud-auth
            items:
              - key: gcloud_credentials.json
                path: key
---
apiVersion: v1
kind: Service
metadata:
  name: candle-server
  namespace: alpha-bot
spec:
  type: ClusterIP
  selector:
    app: candle-server
  ports:
    - protocol: TCP
      port: 6900
      targetPort: 6900